# WORLD_SNAPSHOT BUILDER PROMPT (v0)

Ты — генератор WORLD_SNAPSHOT.md.
Ты не рассуждаешь и не интерпретируешь — ты СБОРЩИК.

## Цель
Собрать файл `WORLD_SNAPSHOT.md`, который является
минимально достаточным контекстом для модели без памяти.

## Источники
Используй ТОЛЬКО следующие источники:
- папку `LORE/`
- папку `DATA/`

Игнорируй:
- `old_lore/`
- `old_data/`
- `GAME/`
- историю чата
- любые предположения

## Общие правила
- Не добавляй ничего от себя.
- Если данных нет — явно укажи `TBD`.
- Не интерпретируй конфликты — только фиксируй.
- Не создавай новые термины или сущности.
- Не сокращай определения.

## Обязательные секции (Snapshot v0)

Сгенерируй файл СТРОГО в следующей структуре:

1. `# WORLD SNAPSHOT`
   - пометка: «Автоматически сгенерированный слепок мира. Не редактируется вручную.»

2. `## Назначение` — вставь дословно, не меняй:
   ```
   Минимально достаточный контекст для модели без памяти. Ответ = f(контекст, вопрос); здесь — весь контекст, на который можно опираться.

   Снапшот даёт модели возможность:

   - **Не додумывать** — всего, чего нет в снапшоте или в LORE/DATA, не существует как факт; при отсутствии данных — TBD или запрос к пользователю.
   - **Не ссылаться на прошлые сообщения** — история чата не источник истины; всё нужное для ответа — в этом файле и в LORE/, DATA/.
   - **Понимать границы допустимого** — см. раздел «Ограничения и допущения»; за их пределами — только с явной пометкой IDEA.
   - **Знать терминологию в одном значении** — глоссарий ниже задаёт единственное рабочее значение термина в этом контексте.
   - **Знать, где истина при конфликте** — источник истины: LORE/ и DATA/; при расхождении снапшота с ними приоритет у LORE/DATA; при конфликте между LORE и DATA — не обходить, фиксировать и разрешать правкой источников.
   ```

3. `## Meta`
   - Commit: (если неизвестно — `unknown`)
   - Built at: (UTC now)
   - Source of truth: LORE/, DATA/

4. `## Правила чтения` — вставь дословно:
   - Этот файл — производное от LORE/ и DATA/. Истина в источниках.
   - При противоречии LORE и DATA — не интерпретировать, выявить и править источники.

5. `## Глоссарий (актуальный)`
   - Включи ВСЕ термины из `LORE/glossary/`
   - CANON — без пометок
   - DRAFT / IDEA — с явной пометкой статуса
   - `_core.md` включай в конец раздела

6. `## Характеристики` (или `## Ключевые сущности мира` → Характеристики, Королевства, Фракции)
   - Если есть отдельные файлы в DATA — используй их
   - Иначе перечисли списком с `TBD`

7. `## Королевства`
   - Если есть структурные данные в DATA — перечисли
   - Иначе напиши `TBD`

8. `## Ограничения и допущения`
   - Нет в snapshot → не существует
   - Не вводить новый канон
   - При отсутствии данных — `TBD` или запрос

## Формат вывода
- Один файл: `WORLD_SNAPSHOT.md`
- Markdown
- Без пояснений вне файла
- Без комментариев

НАЧИНАЙ ГЕНЕРАЦИЮ.