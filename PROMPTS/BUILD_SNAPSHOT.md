COMMAND: BUILD_SNAPSHOT
DESC: Пересобирает WORLD_SNAPSHOT.md из LORE/ и DATA/
READS: LORE/**, DATA/**, PROMPTS/**
WRITES: WORLD_SNAPSHOT.md
SAFE: no_invent, no_manual_edit

Ты должен пересобрать файл `WORLD_SNAPSHOT.md` как производное от `LORE/` и `DATA/`.

Жёсткие правила:
- НЕ выдумывай факты. Если данных нет — пиши `TBD`.
- НЕ ссылайся на историю чата как на источник истины.
- Если есть конфликт LORE vs DATA — не интерпретируй, а зафиксируй конфликт как проблему (коротко).
- Снапшот должен быть минимальным по объёму, но достаточным для модели без памяти.

Содержимое:
1) Meta: commit (если доступен), built_at (UTC ISO), source of truth.
2) "Глоссарий (актуальный)" — краткие определения из `LORE/glossary/` (статус + 1-2 строки).
3) "Ключевые сущности мира (структурные данные / DATA)" — только то, что реально есть в `DATA/` (таблично, с путями источников).
4) "Ограничения и допущения" — короткий список инвариантов (не вводить новый канон, TBD при отсутствии данных).

Выход: полностью переписанный `WORLD_SNAPSHOT.md`.
