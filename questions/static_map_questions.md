# Вопросы по Переходу на Статическую Карту

**Цель:** Понять требования и спланировать переход от генерации к статической карте.

---

## 🗺️ Общие Вопросы

### 1. Масштаб и Размер
- **Сколько узлов планируется?** (10, 50, 100, 500+)
MVP: ~100–250 узлов (как сейчас, но структурнее)
Версия 1: ~300–600
Дальше: 1000+ возможно, но не цель прямо сейчас
Карта будет расширяться постепенно, поэтому нужен формат, который легко дополнять без переделок.
- **Сколько королевств?** (остаётся 8 или меняется?)
На старте 8 “лепестков” + нейтральный центр как “зона”.
В коде “королевство” не жёсткая сущность лора, а владение: faction/kingdom = владелец набора узлов.
В будущем factions могут появляться/исчезать → не привязывать структуру карты к фиксированному числу королевств.
- **Какая плотность узлов?** (густо/редко, равномерно/скоплениями)
Неравномерная: кластеры (населённые зоны) + коридоры дорог (узлы дороги/точки маршрута) + разреженные “пустые” зоны.
Да, нужны “road nodes” (узлы-повороты), чтобы:
делать дороги не прямыми
размещать события “по пути”
добавлять альтернативные маршруты (обход опасностей)
- **Будет ли карта расширяться позже?** (DLC, обновления)
Да (апдейты/DLC), поэтому:
карты версионируются
узлы имеют стабильные ID
добавления — append-only по возможности (не ломать сохранения в будущем)

### 2. Структура и Организация
- **Нужна ли иерархия?** (Capital → Cities → Outposts)
Жёсткая иерархия не нужна как правило, но нужен минимальный набор типов/тегов, чтобы правила дорог/ивентов было куда прикреплять.
Предложение: сейчас использовать tags/roles, а “тип узла” держать техническим минимумом.
- **Нужны ли специальные зоны?** (нейтральные, опасные, ресурсные)
Позже. Сейчас достаточно поддержать поле tags/zone_id в структуре, но не использовать
- **Нужны ли узлы в центре?** (один "Center" или несколько узлов?)
Да: центр — не один узел, а сеть дорожных/ландшафтных узлов, чтобы:
дать вариативность маршрутов
делать “опасные короткие пути” через центр
располагать уникальные события/аномалии
- **Как организовать связи между королевствами?** (через центр, напрямую, через границы)
Это часть карты: связи бывают:
прямые пограничные дороги
обходные через нейтральные зоны/центр
Караваны/отряды — это юниты, а не “связи поселений”.

---

## 🛠️ Создание и Редактирование

### 3. Инструменты
- **Как создавать карту?** (вручную в JSON, визуальный редактор, внешний инструмент)
Сейчас вручную в JSON (быстро начать).
Нужен отдельный “просмотрщик” карты (не запуск игры), чтобы проверять связность/плотность/маршруты.
- **Нужен ли редактор карты в игре?** (для разработки или для модов)
нет
- **Какой формат удобнее?** (JSON, YAML, отдельные файлы для узлов/рёбер)
у меня нет предпочтений, можем обсудить
- **Нужна ли валидация?** (проверка связности, отсутствие дубликатов)
В MVP можно без строгой, но нужен хотя бы скрипт-проверка:
уникальность ID
ребро ссылается на существующие узлы
нет дублей ребёр (A-B == B-A)
карта связна (или хотя бы большой компонент связности)

### 4. Данные Узлов
- **Какие данные нужны для узла?**
  - ✅ ID, название, позиция (x, y)
  - ✅ Тип узла, королевство, роль
  - ❓ Размер/радиус узла?
  пока все одинаковы, но добавим позже
  - ❓ Визуальные параметры (цвет, иконка)?
  пока все одинаковы, но добавим позже
  - ❓ Ресурсы/специализация?
  пока все одинаковы, но добавим позже
  - ❓ Уровень/важность?
  пока все одинаковы, но добавим позже
  - ❓ Описание/лоре?
  пока все одинаковы, но добавим позже
Минимум для MVP узла:

id
name
pos: {x,y} (для визуализации)
tags: [] (для будущих ролей, событий, биомов)
owner_id или faction_id (можно null)
Ответы на “пока одинаковы, но позже”:
Радиус/размер, иконка, важность, ресурсы, лор — добавить позже как поля, но уже сейчас оставить место:
ui: {icon?, color?, size?}
meta: {importance?, lore?}
но не использовать в логике


### 5. Данные Рёбер
- **Какие данные нужны для ребра?**
  - ✅ От узла, к узлу
  - ❓ Тип дороги? (main, secondary, path, center)
  пока все одинаковы, но добавим позже
  - ❓ Множитель скорости?
  пока все одинаковы, но добавим позже
  - ❓ Длина/расстояние?
  пока все одинаковы, но добавим позже
  - ❓ Визуальные параметры (цвет, толщина)?
  пока все одинаковы, но добавим позже
  - ❓ Условия прохождения? (опасность, плата)
  пока все одинаковы, но добавим позже

В данных ребра храним физику / геометрию

length_km (или length_units, если пока без “км”)
road_type (main/secondary/path/…)
biome или terrain (optional сейчас)
tags (optional)
Важно: length_km можно:
считать автоматически из координат (и иметь глобальный km_per_unit)
или задавать вручную (если хочешь “некривую” географию и ручной контроль)
Стоимость для движения = функция от (отряда, ребра, контекста)
Пример:
time_hours = length_km / ( squad_speed_kmh * road_multiplier * terrain_multiplier * weather_multiplier ) 
risk = danger_base * squad_detection * ...
final_cost_for_astar = time_hours * (1 + risk_weight * risk) + tolls + fatigue ...
Для MVP можно начать супер-просто:
time = length_km / speed_kmh(squad, road_type)
всё остальное потом.
---

## 🎨 Визуализация

### 6. Отображение
- **Нужна ли предварительная визуализация?** (для проверки карты)
Да, можно отдельной функцией, мне продётся много посвятить времени карте и не надо для этого запускать саму игру
- **Какой стиль?** (Battle Brothers, схематичный, детальный)
пока схематичный
- **Нужны ли слои?** (узлы, рёбра, зоны, границы королевств)
пока все одинаковы, но добавим позже
- **Как отображать разные типы узлов/рёбер?** (цвета, размеры, иконки)
пока все одинаковы, но добавим позже

Сейчас схематично: точки+линии.
Слои/цвета позже; сейчас всё одинаковое.

### 7. Координаты и Масштаб
- **В каких координатах?** (абсолютные пиксели, относительные, логические единицы)
предложи варианты
- **Какой размер карты?** (фиксированный, масштабируемый)
battle brothers like
- **Нужна ли привязка к экрану?** (центр, границы, камера)
battle brothers like

фиксированный в “world units”, но масштабируемый камерой (zoom)
границы можно хранить в metadata (min/max x/y)
---

## 🔧 Технические Вопросы

### 8. Интеграция с Кодом
- **Заменяем генератор полностью?** (или оставляем как опцию)
Полностью убрать из runtime, оставить как dev-tool (генерить болванку карты, а потом фиксировать вручную)
- **Как загружать статическую карту?** (из одного файла, из нескольких)
Предложи варианты
- **Нужна ли миграция?** (конвертация существующих карт)
Нет
- **Обратная совместимость?** (старые сохранения)
нет

### 9. Производительность
- **Нужна ли оптимизация?** (для больших карт)
- **Кэширование?** (пути, соседи, расстояния)
- **Ленивая загрузка?** (если карта очень большая)
До ~2000 узлов и ~5000 рёбер обычный граф + A* нормально.

Оптимизации добавить позже:
adjacency list строить один раз при загрузке
кэшировать расстояния/эвристику (если нужно)
кэшировать пути для часто используемых направлений (опционально)

### 10. Расширяемость
- **Моды/кастомные карты?** (пользователи могут добавлять свои)
нет
- **Динамические изменения?** (узлы появляются/исчезают во время игры)
пока не надо
- **События на карте?** (временные узлы, блокировки путей)
road nodes (точки пути)
tags на узлах/рёбрах
возможность временно блокировать ребро (будущее поле blocked_until)

---

## 📋 Структура Данных

### 11. Формат Файла
- **Один файл или несколько?**
  - Вариант A: `static_map.json` - всё в одном файле
  - Вариант B: `nodes.json` + `edges.json` + `metadata.json`
  - Вариант C: Папка с файлами по королевствам/зонам
data/maps/world_v1.json
Внутри:
nodes как dict (удобно редактировать, стабильные ключи)
edges list (меньше шанс дублировать)
metadata (version, bounds, notes)

### 12. Метаданные
- **Что хранить в метаданных?**
  - Версия карты
  - Автор/источник
  - Дата создания/изменения
  - Описание/лоре
  - Параметры генерации (если была)
version
created_at, updated_at
bounds (min_x, max_x, min_y, max_y)
notes
schema_version (чтобы мигрировать формат при росте)

### 13. Валидация
- **Что проверять?**
  - Все узлы имеют уникальные ID
  - Все рёбра ссылаются на существующие узлы
  - Карта связна (нет изолированных узлов)
  - Нет дубликатов рёбер
  - Координаты в допустимом диапазоне

---
Минимальные проверки в dev-script:
уникальные ID
все ссылки валидны
нет дублей ребёр
связность
статистика степени
## 🎯 Приоритеты

### 14. MVP
- **Что минимально нужно для MVP?**
  - Базовые узлы и рёбра?
  - Типы узлов/рёбер?
  - Визуализация?
узлы (id, name, pos)
рёбра (from, to, travel_days)
viewer для карты (клики + путь)
движение юнита по ребру (edge_progress) — если уже делаешь “mount&blade ощущение”
Типы/ресурсы/зоны — позже.

### 15. Постепенное Развитие
- **Что можно добавить позже?**
  - Детализация узлов
  - Специальные зоны
  - Визуальные эффекты
  - Редактор карты

---
road_type + speed multipliers
зоны опасности
событийные триггеры на рёбрах/узлах
биомы и “влияние на стоимость”
отрисовка иконок
## 💡 Предложения

### 16. Инструменты
- **Azgaar.io** - генератор карт (можно экспортировать координаты)
- **Tiled** - редактор карт (поддерживает JSON)
- **Вручную в JSON** - простой, но трудоёмкий
- **Скрипт-конвертер** - из существующей генерации

### 17. Структура
- **Рекомендация:** Один файл `data/maps/static_map.json` с секциями:
  ```json
  {
    "metadata": {...},
    "nodes": [...],
    "edges": [...],
    "kingdoms": {...},
    "zones": [...]
  }
  ```

---

## ❓ Вопросы для Обсуждения

1. **Почему статическая карта?** (контроль, лор, баланс, производительность)
2. **Будет ли карта меняться во время игры?** (динамические узлы, события)
3. **Нужна ли поддержка нескольких карт?** (разные режимы, кампании)
4. **Как часто будет обновляться карта?** (редко/часто, кто редактирует)

---

## 📝 Следующие Шаги

После ответов на вопросы:
1. Определить формат данных
2. Создать структуру файла
3. Написать загрузчик/валидатор
4. Создать инструмент для создания/редактирования
5. Мигрировать существующие данные (если нужно)
6. Обновить код для работы со статической картой
