# Генерация и хранение карты - Готово!

## ✅ Что реализовано

### 1. Генератор карты (`src/utils/map_generator.py`)
- ✅ Читает конфигурацию из `data/map_config.json`
- ✅ Генерирует узлы и рёбра на основе конфига
- ✅ Поддерживает все типы узлов (Capital, City, BorderCastle, Outpost, Center)
- ✅ Генерирует связи (hub-структура, ворота между королевствами)

### 2. Хранение карты (`src/utils/map_storage.py`)
- ✅ Сохранение карты в JSON (`data/generated_map.json`)
- ✅ Загрузка карты из JSON
- ✅ Метаданные (версия, статистика)

### 3. Интеграция
- ✅ `World.generate_from_config()` - генерация из конфига
- ✅ `World.__init__()` - автоматическая загрузка сохранённой карты
- ✅ `Game.initialize()` - инициализация с генерацией карты

### 4. Данные карты (`src/utils/map_data.py`)
- ✅ Столицы королевств (из test.py)
- ✅ Порядок королевств по кругу
- ✅ Позиция центра

## 📊 Результаты генерации

**Текущая карта:**
- **Узлов:** 97 (1 Center + 96 узлов королевств)
- **Рёбер:** 208 (104 уникальных × 2 направления)
- **Королевств:** 8
- **Типы узлов:**
  - Capital: 8
  - City: 16
  - BorderCastle: 24
  - Outpost: 48
  - Center: 1

## 🎮 Использование

### Генерация карты
```bash
python generate_map.py
```

### Запуск игры (автоматически загружает карту)
```bash
python main.py
```

## 🔧 Изменение карты

**Теперь очень просто!**

1. Откройте `data/map_config.json`
2. Измените нужные параметры:
   - Количество узлов (`count`)
   - Типы узлов (добавьте новые в `per_kingdom`)
   - Роли узлов (измените массив `roles`)
   - Параметры генерации (в `generation_params`)
3. Запустите `python generate_map.py`
4. Готово! Новая карта сохранена

**Пример:** Чтобы добавить 2 порта в каждое королевство:
```json
"Port": {
  "count": 2,
  "roles": ["Port"],
  "description": "Порты для морской торговли"
}
```

## 📁 Файлы

- `data/map_config.json` - конфигурация структуры карты
- `data/generated_map.json` - сохранённая сгенерированная карта
- `src/utils/map_generator.py` - генератор карты
- `src/utils/map_storage.py` - сохранение/загрузка
- `src/utils/map_data.py` - данные столиц
- `generate_map.py` - скрипт для генерации

## ✅ Готово к использованию

Карта генерируется, сохраняется и загружается автоматически. Изменения карты теперь делаются через конфиг за минуты!
