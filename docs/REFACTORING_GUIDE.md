# Руководство по изменению карты

## Как легко изменить карту в будущем

### ✅ Текущая архитектура (после улучшений)

**Гибкость:** 🟢 Высокая

Большинство изменений карты теперь делаются через конфигурацию, без изменения кода!

---

## Примеры изменений

### 1. Добавить новый тип узла (например, "Port")

**Шаги:**
1. Откройте `data/map_config.json`
2. Добавьте в `node_structure.per_kingdom`:

```json
"Port": {
  "count": 2,
  "roles": ["Port"],
  "description": "Порты для морской торговли"
}
```

3. Готово! Генератор автоматически создаст узлы.

**Время:** 2-3 минуты

---

### 2. Изменить количество узлов (12 → 20)

**Шаги:**
1. Откройте `data/map_config.json`
2. Измените `count` в нужном типе:

```json
"Outpost": {
  "count": 10,  // было 6
  "roles": ["Farm", "Farm", "Mine", "Lumber", "TradePost", "Ruin", "Port", "Temple", "Barracks", "Market"]
}
```

3. Готово!

**Время:** 1-2 минуты

---

### 3. Добавить новую роль для Outpost

**Шаги:**
1. Откройте `data/map_config.json`
2. Добавьте роль в массив `roles`:

```json
"Outpost": {
  "count": 7,
  "roles": ["Farm", "Farm", "Mine", "Lumber", "TradePost", "Ruin", "Temple"]  // добавили Temple
}
```

3. Готово!

**Время:** 1 минута

---

### 4. Изменить параметры генерации

**Шаги:**
1. Откройте `data/map_config.json`
2. Измените значения в `generation_params`:

```json
"generation_params": {
  "outpost_radius": 200.0,  // было 170.0
  "city_offset": 120.0       // было 95.0
}
```

3. Готово!

**Время:** 1 минута

---

### 5. Изменить структуру связей

**Шаги:**
1. Откройте `data/map_config.json`
2. Измените `connection_rules`:

```json
"connection_rules": {
  "hub_structure": {
    "enabled": false,  // отключить hub-структуру
    "description": "Все узлы соединены со столицей"
  },
  "network_structure": {
    "enabled": true,   // включить сетевую структуру
    "description": "Узлы соединены в сеть"
  }
}
```

3. Обновите `map_generator.py` для поддержки новой структуры (если нужно)

**Время:** 5-15 минут (в зависимости от сложности)

---

## Что НЕ нужно менять в коде

✅ **Типы узлов** — просто строки, добавляйте любые  
✅ **Роли узлов** — просто строки, добавляйте любые  
✅ **Количество узлов** — меняется в конфиге  
✅ **Параметры генерации** — меняются в конфиге  
✅ **Базовые классы** (`Node`, `World`) — не зависят от конкретных типов

---

## Что может потребовать изменения кода

⚠️ **Новая структура связей** — если нужна совершенно новая логика (не hub, не сеть)  
⚠️ **Новая логика генерации позиций** — если нужны специальные алгоритмы размещения  
⚠️ **Специфичные механики для типов узлов** — если нужна особая логика для конкретного типа

---

## Рекомендации

1. **Используйте конфиг** для всех изменений структуры
2. **Добавляйте новые типы/роли** через конфиг, не через код
3. **Тестируйте изменения** через `map_generator.py`
4. **Документируйте** новые типы в `data/map_config.json` (поле `description`)

---

## Итоговая оценка сложности

**Текущая архитектура (после улучшений):**
- 🟢 Добавить тип узла: **2-3 минуты** (только конфиг)
- 🟢 Изменить количество: **1-2 минуты** (только конфиг)
- 🟢 Добавить роль: **1 минута** (только конфиг)
- 🟡 Изменить структуру связей: **5-15 минут** (конфиг + возможно код)
- 🔴 Полностью новая логика генерации: **1-2 часа** (нужен код)

**Было (до улучшений):**
- 🟡 Добавить тип узла: **2-3 часа** (изменение test.py)
- 🟡 Изменить количество: **1-2 часа** (изменение test.py)
- 🟢 Добавить роль: **10 минут** (просто массив)
- 🔴 Изменить структуру связей: **4-6 часов** (переписать логику)

**Улучшение:** В 10-20 раз быстрее для большинства изменений!
