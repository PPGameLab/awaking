# Свойства Сущностей Карты

**Вопрос:** Какие свойства нужны для owners, road_types, biomes, node_tags?

---

## 📊 Анализ Требований

### Текущее состояние:
- `owners` - только `name`
- `road_types` - только список строк
- `biomes` - только список строк
- `node_tags` - только список строк

### Что может понадобиться:

#### 1. Owners (Владельцы/Фракции)
**Сейчас:** `{owner_id: {name}}`

**Потенциальные свойства:**
- `name` - название (уже есть)
- `color` - цвет для визуализации
- `flag` - флаг/герб (путь к изображению)
- `relations` - отношения с другими фракциями (ally/neutral/enemy)
- `culture` - культура
- `religion` - религия
- `economy_type` - тип экономики

**Для MVP:** Только `name` достаточно
**Для будущего:** Легко расширить, добавив поля в объект

---

#### 2. Road Types (Типы дорог)
**Сейчас:** `["main", "secondary", "path", "center"]`

**Потенциальные свойства:**
- `speed_multiplier` - множитель скорости (1.0 для main, 0.8 для path)
- `color` - цвет для визуализации
- `width` - ширина линии
- `danger_level` - уровень опасности
- `cost` - стоимость прохождения (плата)

**Для MVP:** Можно оставить списком, свойства добавить позже
**Для будущего:** Преобразовать в объект `{road_type: {speed_multiplier, color, ...}}`

---

#### 3. Biomes (Биомы)
**Сейчас:** `["plains", "forest", "mountain", "desert", "swamp"]`

**Потенциальные свойства:**
- `speed_multiplier` - влияние на скорость
- `danger_level` - уровень опасности
- `visibility` - видимость
- `color` - цвет для визуализации
- `description` - описание

**Для MVP:** Можно оставить списком
**Для будущего:** Преобразовать в объект

---

#### 4. Node Tags (Теги узлов)
**Сейчас:** `["city", "settlement", "capital", ...]`

**Потенциальные свойства:**
- `icon` - иконка по умолчанию
- `color` - цвет по умолчанию
- `size` - размер по умолчанию
- `description` - описание

**Для MVP:** Можно оставить списком (открытый список)
**Для будущего:** Преобразовать в объект (опционально)

---

## 💡 Рекомендация

### Вариант 1: Расширяемая структура (РЕКОМЕНДУЕТСЯ)

**Сейчас:**
```json
{
  "dictionary": {
    "owners": {
      "Kingdom1": {
        "name": "Королевство Креста"
      }
    },
    "road_types": ["main", "secondary", "path", "center"],
    "biomes": ["plains", "forest"],
    "node_tags": ["city", "settlement"]
  }
}
```

**Позже (когда понадобится):**
```json
{
  "dictionary": {
    "owners": {
      "Kingdom1": {
        "name": "Королевство Креста",
        "color": "#FF0000",
        "relations": {"Kingdom2": "enemy"}
      }
    },
    "road_types": {
      "main": {
        "speed_multiplier": 1.0,
        "color": "#8B4513"
      },
      "secondary": {
        "speed_multiplier": 0.9,
        "color": "#A0522D"
      }
    },
    "biomes": {
      "plains": {
        "speed_multiplier": 1.0,
        "danger_level": 0.1
      }
    },
    "node_tags": {
      "city": {
        "icon": "city.png",
        "size": 20
      }
    }
  }
}
```

**Преимущества:**
- ✅ Обратная совместимость (поддержка и списков, и объектов)
- ✅ Легко расширять без изменения структуры
- ✅ Не нужно заполнять все поля сразу

---

### Вариант 2: Полная структура сразу

**Сейчас:**
```json
{
  "dictionary": {
    "owners": {
      "Kingdom1": {
        "name": "Королевство Креста",
        "color": null,
        "flag": null
      }
    },
    "road_types": {
      "main": {
        "speed_multiplier": 1.0,
        "color": null
      }
    }
  }
}
```

**Преимущества:**
- ✅ Явная структура с самого начала
- ✅ Все поля определены

**Недостатки:**
- ❌ Много null значений
- ❌ Нужно заполнять даже неиспользуемые поля

---

## 🎯 Рекомендация для MVP

**Использовать Вариант 1 (расширяемая структура):**

1. **Owners** - уже объекты, легко добавить поля позже
2. **Road_types, biomes, node_tags** - оставить списками сейчас
3. **Код поддержки** - написать так, чтобы поддерживал и списки, и объекты

**Когда понадобится:**
- Преобразовать списки в объекты
- Добавить нужные свойства
- Обновить код для работы с объектами

**Преимущества:**
- ✅ Не раздуваем данные сейчас
- ✅ Легко расширять позже
- ✅ Обратная совместимость

---

## 📝 План Расширения

### Когда понадобятся свойства:

1. **Road_types** - когда начнём использовать speed_multiplier для движения
2. **Biomes** - когда начнём учитывать влияние биомов на движение
3. **Node_tags** - когда начнём использовать иконки/цвета для узлов
4. **Owners** - когда начнём использовать отношения/цвета фракций

### Как расширять:

1. Обновить формат в `dictionary` (список → объект)
2. Обновить код для поддержки обоих форматов (обратная совместимость)
3. Обновить валидатор
4. Обновить документацию

---

## ✅ Вывод

**Для MVP:**
- ✅ Owners - уже объекты, можно добавить поля позже
- ✅ Road_types, biomes, node_tags - оставить списками
- ✅ Код должен поддерживать оба формата (список и объект)

**Позже:**
- Когда понадобятся свойства - преобразовать в объекты
- Добавить нужные поля
- Обновить код

**Это удобно добавить позже** - структура позволяет легко расширять без breaking changes.

---

## 🔧 Реализация Поддержки Обоих Форматов

**Пример кода для поддержки списков и объектов:**

```python
def get_road_type_property(road_type: str, property_name: str, default_value):
    """Получает свойство типа дороги (поддерживает и список, и объект)"""
    road_types = dictionary.get("road_types", [])
    
    if isinstance(road_types, dict):
        # Новый формат (объект)
        road_data = road_types.get(road_type, {})
        return road_data.get(property_name, default_value)
    else:
        # Старый формат (список) - используем дефолт
        return default_value

# Использование:
speed_mult = get_road_type_property("main", "speed_multiplier", 1.0)
color = get_road_type_property("main", "color", "#8B4513")
```

Это позволит легко расширять без breaking changes.
