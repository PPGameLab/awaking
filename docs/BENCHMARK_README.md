# Скрипт для Тестирования Производительности

Скрипт `benchmark_performance.py` позволяет тестировать производительность игры и запускать его параллельно в консоли.

---

## 🚀 Быстрый Старт

### Базовый запуск (загружает сохранённую карту):
```bash
python benchmark_performance.py
```

### Генерация новой карты:
```bash
python benchmark_performance.py --generate
```

### Без визуализации (быстрее):
```bash
python benchmark_performance.py --no-viz
```

### С меньшим количеством итераций pathfinding:
```bash
python benchmark_performance.py --iterations 50
```

---

## 📊 Что Тестируется

1. **Генерация/Загрузка карты**
   - Время генерации/загрузки
   - Использование памяти
   - Количество узлов и рёбер

2. **Pathfinding (A* алгоритм)**
   - Среднее время поиска пути
   - Минимум/максимум
   - Общее время для N итераций

3. **Визуализация** (опционально)
   - Простая визуализация
   - Battle Brothers стиль

4. **Память**
   - Текущее использование
   - Пиковое использование

---

## 🎯 Параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--generate` | Генерировать новую карту вместо загрузки | Загружает сохранённую |
| `--no-viz` | Не тестировать визуализацию (быстрее) | Тестирует визуализацию |
| `--iterations N` | Количество итераций pathfinding | 100 |
| `--output FILE` | Файл для сохранения результатов | `benchmark_results.json` |

---

## 📁 Результаты

Результаты автоматически сохраняются в `benchmark_results.json` (или указанный файл).

**Формат:**
```json
[
  {
    "timestamp": "2024-01-15T10:30:00",
    "results": {
      "map_load_time_ms": 4.16,
      "pathfinding_avg_ms": 0.094,
      "nodes_count": 97,
      ...
    }
  }
]
```

**Хранится:** Последние 10 результатов для сравнения.

---

## 💡 Примеры Использования

### Тест текущей карты:
```bash
python benchmark_performance.py --no-viz
```

### Тест генерации новой карты:
```bash
python benchmark_performance.py --generate --no-viz
```

### Быстрый тест pathfinding:
```bash
python benchmark_performance.py --no-viz --iterations 20
```

### Полный тест с визуализацией:
```bash
python benchmark_performance.py
```

### Сравнение производительности (запустить несколько раз):
```bash
# Запуск 1
python benchmark_performance.py --no-viz --output test1.json

# Запуск 2 (после изменений)
python benchmark_performance.py --no-viz --output test2.json
```

---

## 📊 Оценка Производительности

Скрипт автоматически оценивает производительность:

- 🟢 **Отлично** - Производительность отличная
- 🟢 **Хорошо** - Производительность хорошая
- 🟡 **Приемлемо** - Производительность приемлемая, но можно улучшить
- 🔴 **Медленно** - Производительность низкая, нужны оптимизации

**Критерии:**

| Метрика | Отлично | Хорошо | Приемлемо | Медленно |
|---------|---------|--------|-----------|----------|
| Pathfinding | <1 мс | <5 мс | <10 мс | >10 мс |
| Визуализация (BB) | <40 мс | <80 мс | <150 мс | >150 мс |
| Загрузка карты | <50 мс | <100 мс | <200 мс | >200 мс |
| Генерация карты | <200 мс | <500 мс | <1500 мс | >1500 мс |

---

## 🔄 Параллельный Запуск

Можно запускать несколько экземпляров параллельно для мониторинга:

```bash
# Терминал 1
python benchmark_performance.py --no-viz --output test1.json

# Терминал 2
python benchmark_performance.py --no-viz --output test2.json

# Терминал 3
python benchmark_performance.py --generate --no-viz --output test3.json
```

---

## 📝 Примечания

- Скрипт автоматически устанавливает UTF-8 для вывода
- Результаты сохраняются автоматически
- Можно запускать параллельно без конфликтов (если использовать разные output файлы)
- Визуализация может быть медленной, используйте `--no-viz` для быстрых тестов

---

## 🐛 Устранение Проблем

**Ошибка: "No map loaded"**
- Запустите `python generate_map.py` сначала
- Или используйте `--generate` для генерации новой карты

**Ошибка: "UnicodeEncodeError"**
- Скрипт должен автоматически устанавливать UTF-8
- Если проблема остаётся, проверьте кодировку консоли

**Медленная визуализация**
- Используйте `--no-viz` для пропуска визуализации
- Визуализация Battle Brothers стиля медленнее простой

---

## ✅ Готово!

Теперь вы можете тестировать производительность игры в любое время!
