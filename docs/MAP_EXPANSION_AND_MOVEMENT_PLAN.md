# План Расширения Карты и Реализации Движения

**Цель:** Значительно расширить карту (в 10 раз), реализовать движение по карте, постепенно добавить графику.

**Фокус:** Только карта и движение, без ресурсов, взаимодействия и других отрядов.

---

## 📊 Текущее Состояние

**Текущая карта:**
- Узлов: 97 (1 Center + 96 узлов королевств)
- Узлов на королевство: 12
- Рёбер: 208

**Целевая карта:**
- Узлов: ~1000 (в 10 раз больше)
- Узлов на королевство: ~120
- Рёбер: ~2000-3000

---

## 🎯 Этапы Реализации

### Этап 1: Расширение Карты (1-2 недели)

**Цель:** Увеличить карту до ~1000 узлов, добавить новые типы узлов и пространства.

#### Шаг 1.1: Обновление конфигурации карты (1-2 дня)

**Задачи:**
- [ ] Обновить `data/map_config.json` с новыми типами узлов
- [ ] Увеличить количество узлов на королевство до ~120
- [ ] Добавить новые типы узлов:
  - Village (деревни) - 10-15 на королевство
  - Fortress (крепости) - 3-5 на королевство
  - Ruin (руины) - 5-10 на королевство
  - Camp (лагеря) - 5-10 на королевство
  - Crossroad (перекрёстки) - 10-15 на королевство
  - Watchtower (дозорные башни) - 5-8 на королевство
  - More Outpost с разными ролями - 20-30 на королевство

**Структура узлов на королевство (примерно):**
- Capital: 1
- City: 8-10
- BorderCastle: 10-12
- Village: 10-15
- Fortress: 3-5
- Ruin: 5-10
- Camp: 5-10
- Crossroad: 10-15
- Watchtower: 5-8
- Outpost: 20-30
- **Итого:** ~120 узлов на королевство

**Результат:** Конфигурация готова для генерации большой карты.

---

#### Шаг 1.2: Обновление генератора карты (2-3 дня)

**Задачи:**
- [ ] Добавить генерацию новых типов узлов в `src/utils/map_generator.py`
- [ ] Обновить логику размещения узлов (более равномерное распределение)
- [ ] Добавить новые роли для узлов
- [ ] Обновить структуру связей (не только hub, но и региональные связи)

**Новые методы генерации:**
- `_generate_villages()` - деревни вокруг городов
- `_generate_fortresses()` - крепости на границах
- `_generate_ruins()` - руины в опасных зонах
- `_generate_camps()` - лагеря в нейтральных зонах
- `_generate_crossroads()` - перекрёстки на дорогах
- `_generate_watchtowers()` - дозорные башни

**Структура связей:**
- Hub-структура (все к столице) - оставить
- Региональные связи (города к столице, деревни к городам)
- Дороги между соседними узлами
- Перекрёстки как узлы связи

**Результат:** Генератор может создавать карту с ~1000 узлов.

---

#### Шаг 1.3: Тестирование и оптимизация генерации (1-2 дня)

**Задачи:**
- [ ] Протестировать генерацию большой карты
- [ ] Проверить производительность (время генерации, память)
- [ ] Оптимизировать при необходимости
- [ ] Проверить pathfinding на большой карте

**Ожидаемые показатели:**
- Время генерации: 1-3 секунды
- Память: ~500 KB - 1 MB (JSON)
- Pathfinding: 10-20 мс (приемлемо)

**Результат:** Большая карта генерируется и работает.

---

### Этап 2: Движение по Рёбрам (1 неделя)

**Цель:** Реализовать движение отрядов по рёбрам с промежуточными позициями.

#### Шаг 2.1: Обновление Squad класса (1 день)

**Задачи:**
- [ ] Добавить поля для движения по рёбрам в `src/entities/unit.py`:
  - `current_edge: Optional[Tuple[str, str]]` - текущее ребро
  - `edge_progress: float` - прогресс по ребру (0.0-1.0)
  - `is_moving: bool` - флаг движения
  - `movement_speed: float` - скорость движения

**Код:**
```python
@dataclass
class Squad:
    # ... существующие поля ...
    
    # Движение по рёбрам
    current_edge: Optional[Tuple[str, str]] = None  # (node_from, node_to)
    edge_progress: float = 0.0  # 0.0 = начало, 1.0 = конец
    is_moving: bool = False
```

**Результат:** Squad поддерживает движение по рёбрам.

---

#### Шаг 2.2: Логика движения в Game (2-3 дня)

**Задачи:**
- [ ] Реализовать `_update_squad()` в `src/core/game.py`
- [ ] Логика начала движения (из узла на ребро)
- [ ] Логика движения по ребру (обновление `edge_progress`)
- [ ] Логика прибытия в узел (с ребра в узел)
- [ ] Обновление пути при достижении узла

**Логика:**
1. Если отряд в узле и есть путь → начинаем движение по первому ребру
2. Если отряд на ребре → обновляем `edge_progress` по скорости
3. Если `edge_progress >= 1.0` → прибываем в узел, убираем из пути

**Результат:** Отряды могут двигаться между узлами.

---

#### Шаг 2.3: Визуализация движения (matplotlib) (1-2 дня)

**Задачи:**
- [ ] Обновить визуализацию карты для показа отрядов
- [ ] Показывать отряд на ребре (интерполяция позиции)
- [ ] Анимация движения (обновление позиции)

**Метод:**
```python
def get_squad_position(squad: Squad) -> Tuple[float, float]:
    """Возвращает текущую позицию отряда"""
    if squad.current_edge:
        # Позиция на ребре
        node_from = world.get_node(squad.current_edge[0])
        node_to = world.get_node(squad.current_edge[1])
        return lerp(node_from.pos, node_to.pos, squad.edge_progress)
    else:
        # Позиция в узле
        node = world.get_node(squad.current_node_id)
        return node.pos
```

**Результат:** Движение визуально видно на карте.

---

### Этап 3: Переход к Графике (2-3 недели)

**Цель:** Постепенно заменить matplotlib на pygame/pyglet для интерактивной графики.

#### Шаг 3.1: Выбор и настройка графической библиотеки (1 день)

**Варианты:**
- **pygame** (рекомендуется) - простой, популярный, хорошая документация
- **pyglet** - легковесный, более гибкий
- **arcade** - современный, но менее популярный

**Рекомендация:** pygame

**Задачи:**
- [ ] Добавить `pygame` в `requirements.txt`
- [ ] Создать базовую структуру для графики
- [ ] Настроить окно и основной цикл

**Результат:** Графическая библиотека выбрана и настроена.

---

#### Шаг 3.2: Базовый рендеринг карты (pygame) (3-5 дней)

**Задачи:**
- [ ] Создать `src/ui/map_renderer.py` для рендеринга карты
- [ ] Рендеринг узлов (точки/иконки)
- [ ] Рендеринг рёбер (линии)
- [ ] Рендеринг отряда (точка на карте)
- [ ] Камера и масштабирование

**Структура:**
```python
class MapRenderer:
    def __init__(self, screen, world):
        self.screen = screen
        self.world = world
        self.camera_x = 0
        self.camera_y = 0
        self.zoom = 1.0
    
    def render(self, squads: List[Squad]):
        """Рендерит карту и отряды"""
        # Рендеринг рёбер
        # Рендеринг узлов
        # Рендеринг отрядов
```

**Результат:** Карта отображается в pygame.

---

#### Шаг 3.3: Интерактивность (3-5 дней)

**Задачи:**
- [ ] Клики по узлам (выбор узла)
- [ ] Клики по карте (установка цели движения)
- [ ] Обработка клавиатуры (пауза, ускорение, масштаб)
- [ ] Информация о узле при наведении

**Управление:**
- ЛКМ по узлу → информация о узле
- ПКМ по карте → установить цель движения
- Пробел → пауза
- +/- → ускорение/замедление
- Колесо мыши → масштаб

**Результат:** Карта интерактивна.

---

#### Шаг 3.4: Визуализация движения (2-3 дня)

**Задачи:**
- [ ] Плавная анимация движения отряда
- [ ] Визуализация пути (линия от текущей позиции до цели)
- [ ] Обновление позиции отряда в реальном времени

**Результат:** Движение плавно и визуально приятно.

---

#### Шаг 3.5: Улучшение визуализации (3-5 дней)

**Задачи:**
- [ ] Разные иконки для разных типов узлов
- [ ] Цвета для королевств
- [ ] Дороги (толщина зависит от типа)
- [ ] Ландшафт (опционально, простой фон)

**Результат:** Карта выглядит лучше и информативнее.

---

## 📋 Детальный План по Дням

### Неделя 1: Расширение карты

**День 1-2:** Обновление конфигурации
- Обновить `map_config.json`
- Добавить новые типы узлов
- Увеличить количество узлов

**День 3-5:** Обновление генератора
- Добавить методы генерации новых типов узлов
- Обновить логику размещения
- Обновить структуру связей

**День 6-7:** Тестирование
- Протестировать генерацию
- Проверить производительность
- Оптимизировать при необходимости

---

### Неделя 2: Движение

**День 8:** Обновление Squad
- Добавить поля для движения по рёбрам

**День 9-11:** Логика движения
- Реализовать `_update_squad()`
- Логика начала движения
- Логика движения по ребру
- Логика прибытия в узел

**День 12-14:** Визуализация движения (matplotlib)
- Обновить визуализацию
- Показывать отряд на ребре
- Анимация движения

---

### Неделя 3-4: Переход к графике

**День 15:** Выбор библиотеки
- Добавить pygame в requirements
- Настроить базовую структуру

**День 16-20:** Базовый рендеринг
- Создать MapRenderer
- Рендеринг узлов и рёбер
- Рендеринг отряда
- Камера и масштабирование

**День 21-25:** Интерактивность
- Клики по узлам
- Клики по карте
- Обработка клавиатуры
- Информация о узле

**День 26-28:** Визуализация движения
- Плавная анимация
- Визуализация пути
- Обновление в реальном времени

---

### Неделя 5: Улучшения

**День 29-33:** Улучшение визуализации
- Разные иконки для узлов
- Цвета для королевств
- Дороги
- Ландшафт (опционально)

---

## 🎯 Критерии Успеха

### После Этапа 1 (Расширение карты):
- ✅ Карта содержит ~1000 узлов
- ✅ Есть новые типы узлов (Village, Fortress, Ruin, etc.)
- ✅ Генерация работает за <3 секунды
- ✅ Pathfinding работает за <20 мс

### После Этапа 2 (Движение):
- ✅ Отряды могут двигаться между узлами
- ✅ Движение визуально видно
- ✅ Отряды прибывают в узлы корректно

### После Этапа 3 (Графика):
- ✅ Карта отображается в pygame
- ✅ Можно кликать по узлам
- ✅ Можно устанавливать цель движения
- ✅ Движение плавное и визуально приятное

---

## 🔧 Технические Детали

### Новые типы узлов

**Village (деревни):**
- Расположение: вокруг городов
- Количество: 10-15 на королевство
- Роли: Farm, Lumber, TradePost

**Fortress (крепости):**
- Расположение: на границах, стратегические точки
- Количество: 3-5 на королевство
- Роли: Fortress, Watchtower

**Ruin (руины):**
- Расположение: опасные зоны, центр
- Количество: 5-10 на королевство
- Роли: Ruin, MonsterNest

**Camp (лагеря):**
- Расположение: нейтральные зоны
- Количество: 5-10 на королевство
- Роли: Camp, RestPoint

**Crossroad (перекрёстки):**
- Расположение: на дорогах между узлами
- Количество: 10-15 на королевство
- Роли: Crossroad, TradeRoute

**Watchtower (дозорные башни):**
- Расположение: на границах, возвышенностях
- Количество: 5-8 на королевство
- Роли: Watchtower, SignalTower

---

### Структура связей для большой карты

**Уровень 1: Hub-структура**
- Все узлы соединены со столицей (оставить для быстрого доступа)

**Уровень 2: Региональные связи**
- Города соединены между собой
- Деревни соединены с ближайшими городами
- Крепости соединены с городами и друг с другом

**Уровень 3: Дороги**
- Перекрёстки соединены с соседними узлами
- Дороги между соседними узлами (если расстояние < порога)

**Уровень 4: Специальные связи**
- Ворота между королевствами
- Центр соединён с воротами

---

### Оптимизации для большой карты

**Pathfinding:**
- Кэширование результатов
- Пространственное разбиение (если нужно)

**Визуализация:**
- LOD (Level of Detail) - детализация по масштабу
- Рендеринг только видимых узлов
- Кэширование отрисовки

**Генерация:**
- Ленивая загрузка узлов (если нужно)
- Оптимизация алгоритмов генерации

---

## 📝 Файлы для Создания/Обновления

### Новые файлы:
- `src/ui/map_renderer.py` - рендеринг карты в pygame
- `src/ui/game_window.py` - главное окно игры
- `src/ui/camera.py` - камера и масштабирование

### Обновляемые файлы:
- `data/map_config.json` - новая конфигурация
- `src/utils/map_generator.py` - новые методы генерации
- `src/entities/unit.py` - поля для движения
- `src/core/game.py` - логика движения
- `requirements.txt` - добавить pygame

---

## ✅ Итоговый План

**Время:** 4-5 недель

**Этапы:**
1. Расширение карты (1-2 недели)
2. Движение по рёбрам (1 неделя)
3. Переход к графике (2-3 недели)

**Результат:**
- Карта с ~1000 узлов
- Движение по карте работает
- Графический интерфейс (pygame)
- Интерактивность (клики, управление)

**Фокус:** Только карта и движение, без ресурсов и взаимодействия.

---

## 🚀 Начало Работы

**Первые шаги:**
1. Обновить `data/map_config.json` с новыми типами узлов
2. Увеличить количество узлов
3. Протестировать генерацию
4. Начать реализацию движения

**Следующий шаг:** Обновление конфигурации карты.
